// Copyright 2026 jeff-kittrell. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package mcp

import (
	"context"
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"strings"
	"time"

	mcplib "github.com/mark3labs/mcp-go/mcp"
	"github.com/mark3labs/mcp-go/server"
	"expedia-cars-pp-cli/internal/cli"
	"expedia-cars-pp-cli/internal/cliutil"
	"expedia-cars-pp-cli/internal/client"
	"expedia-cars-pp-cli/internal/config"
	"expedia-cars-pp-cli/internal/mcp/cobratree"
	"expedia-cars-pp-cli/internal/store"
)

// RegisterTools registers all API operations as MCP tools.
func RegisterTools(s *server.MCPServer) {
	s.AddTool(
		mcplib.NewTool(".well-known_list_web_identity",
			mcplib.WithDescription("GET /.well-known/web-identity. Returns the web_identity."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/.well-known/web-identity", []mcpParamBinding{ }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("cl_create_2x2.json",
			mcplib.WithDescription("POST /cl/2x2.json. Returns the new 2x2.json."),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/cl/2x2.json", []mcpParamBinding{ }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("ds_get_pwa",
			mcplib.WithDescription("GET /ds/api/v1/toolkit/page.Car-Search/{id}/en_US/pwa. Required: id. Optional: brand. Returns the pwa."),
			mcplib.WithString("brand", mcplib.Description("")),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("The id path segment")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/ds/api/v1/toolkit/page.Car-Search/{id}/en_US/pwa", []mcpParamBinding{{PublicName: "brand", WireName: "brand", Location: "query"},{PublicName: "id", WireName: "id", Location: "path"}, }, []string{"id", }),
	)
	s.AddTool(
		mcplib.NewTool("egcs_create_collect",
			mcplib.WithDescription("POST /egcs/v2/collect. Returns the new collect."),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/egcs/v2/collect", []mcpParamBinding{ }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("getconfig_list_sodar",
			mcplib.WithDescription("GET /getconfig/sodar. Optional: sjk, st, sv (plus 2 more). Returns the sodar."),
			mcplib.WithNumber("sjk", mcplib.Description("")),
			mcplib.WithString("st", mcplib.Description("")),
			mcplib.WithNumber("sv", mcplib.Description("")),
			mcplib.WithString("tid", mcplib.Description("")),
			mcplib.WithString("tv", mcplib.Description("")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/getconfig/sodar", []mcpParamBinding{{PublicName: "sjk", WireName: "sjk", Location: "query"},{PublicName: "st", WireName: "st", Location: "query"},{PublicName: "sv", WireName: "sv", Location: "query"},{PublicName: "tid", WireName: "tid", Location: "query"},{PublicName: "tv", WireName: "tv", Location: "query"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("graphql_create_graphql",
			mcplib.WithDescription("POST /graphql. Returns the new graphql."),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/graphql", []mcpParamBinding{ }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("gsi_list_listaccounts",
			mcplib.WithDescription("GET /gsi/fedcm/listaccounts. Returns the listaccounts."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/gsi/fedcm/listaccounts", []mcpParamBinding{ }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("gsi_list_passive.json",
			mcplib.WithDescription("GET /gsi/fedcm/config/passive.json. Returns the passive.json."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/gsi/fedcm/config/passive.json", []mcpParamBinding{ }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("gsi_list_status",
			mcplib.WithDescription("GET /gsi/status. Optional: bs, cas, client_id (plus 1 more). Returns the status."),
			mcplib.WithString("bs", mcplib.Description("")),
			mcplib.WithString("cas", mcplib.Description("")),
			mcplib.WithString("client_id", mcplib.Description("")),
			mcplib.WithBoolean("has_opted_out_fedcm", mcplib.Description("")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/gsi/status", []mcpParamBinding{{PublicName: "bs", WireName: "bs", Location: "query"},{PublicName: "cas", WireName: "cas", Location: "query"},{PublicName: "client_id", WireName: "client_id", Location: "query"},{PublicName: "has_opted_out_fedcm", WireName: "has_opted_out_fedcm", Location: "query"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("k4_a_z50_dsywo_m_create__np_j2or",
			mcplib.WithDescription("POST /k4AZ50DsywoM/SlBOW8/_KqZ0-/Sz3D2J7Quhfi/LkNVBTN0TwM/DUUTBl/NpJ2or. Returns the new NpJ2or."),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/k4AZ50DsywoM/SlBOW8/_KqZ0-/Sz3D2J7Quhfi/LkNVBTN0TwM/DUUTBl/NpJ2or", []mcpParamBinding{ }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("maps_list_gen_204",
			mcplib.WithDescription("GET /maps/api/mapsjs/gen_204. Optional: csp_test. Returns the gen_204."),
			mcplib.WithBoolean("csp_test", mcplib.Description("")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/maps/api/mapsjs/gen_204", []mcpParamBinding{{PublicName: "csp_test", WireName: "csp_test", Location: "query"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("maps_list_js",
			mcplib.WithDescription("GET /maps/api/js. Optional: callback, channel, language (plus 3 more). Returns the js."),
			mcplib.WithString("callback", mcplib.Description("")),
			mcplib.WithString("channel", mcplib.Description("")),
			mcplib.WithString("language", mcplib.Description("")),
			mcplib.WithString("libraries", mcplib.Description("")),
			mcplib.WithString("region", mcplib.Description("")),
			mcplib.WithString("v", mcplib.Description("")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/maps/api/js", []mcpParamBinding{{PublicName: "callback", WireName: "callback", Location: "query"},{PublicName: "channel", WireName: "channel", Location: "query"},{PublicName: "language", WireName: "language", Location: "query"},{PublicName: "libraries", WireName: "libraries", Location: "query"},{PublicName: "region", WireName: "region", Location: "query"},{PublicName: "v", WireName: "v", Location: "query"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("pagead_get_conversion",
			mcplib.WithDescription("GET /pagead/conversion/{id}/. Required: id. Optional: async, auid, bg (plus 38 more). Returns the conversion."),
			mcplib.WithNumber("async", mcplib.Description("")),
			mcplib.WithString("auid", mcplib.Description("")),
			mcplib.WithString("bg", mcplib.Description("")),
			mcplib.WithNumber("capi", mcplib.Description("")),
			mcplib.WithString("category", mcplib.Description("")),
			mcplib.WithNumber("cv", mcplib.Description("")),
			mcplib.WithString("data", mcplib.Description("")),
			mcplib.WithNumber("dma", mcplib.Description("")),
			mcplib.WithString("ec_mode", mcplib.Description("")),
			mcplib.WithString("eme", mcplib.Description("")),
			mcplib.WithString("en", mcplib.Description("")),
			mcplib.WithNumber("fmt", mcplib.Description("")),
			mcplib.WithNumber("frm", mcplib.Description("")),
			mcplib.WithNumber("fst", mcplib.Description("")),
			mcplib.WithNumber("gap.gtb", mcplib.Description("")),
			mcplib.WithString("gcd", mcplib.Description("")),
			mcplib.WithString("gcl_ctr", mcplib.Description("")),
			mcplib.WithString("gcs", mcplib.Description("")),
			mcplib.WithString("gtm", mcplib.Description("")),
			mcplib.WithNumber("gtm_ee", mcplib.Description("")),
			mcplib.WithString("guid", mcplib.Description("")),
			mcplib.WithString("hn", mcplib.Description("")),
			mcplib.WithString("id", mcplib.Required(), mcplib.Description("The id path segment")),
			mcplib.WithString("label", mcplib.Description("")),
			mcplib.WithNumber("npa", mcplib.Description("")),
			mcplib.WithString("pscdl", mcplib.Description("")),
			mcplib.WithNumber("random", mcplib.Description("")),
			mcplib.WithNumber("rcb", mcplib.Description("")),
			mcplib.WithString("tag_exp", mcplib.Description("")),
			mcplib.WithString("tiba", mcplib.Description("")),
			mcplib.WithNumber("u_h", mcplib.Description("")),
			mcplib.WithNumber("u_w", mcplib.Description("")),
			mcplib.WithString("uaa", mcplib.Description("")),
			mcplib.WithNumber("uab", mcplib.Description("")),
			mcplib.WithString("uafvl", mcplib.Description("")),
			mcplib.WithString("uam", mcplib.Description("")),
			mcplib.WithNumber("uamb", mcplib.Description("")),
			mcplib.WithString("uap", mcplib.Description("")),
			mcplib.WithString("uapv", mcplib.Description("")),
			mcplib.WithNumber("uaw", mcplib.Description("")),
			mcplib.WithString("url", mcplib.Description("")),
			mcplib.WithString("userId", mcplib.Description("")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/pagead/conversion/{id}/", []mcpParamBinding{{PublicName: "async", WireName: "async", Location: "query"},{PublicName: "auid", WireName: "auid", Location: "query"},{PublicName: "bg", WireName: "bg", Location: "query"},{PublicName: "capi", WireName: "capi", Location: "query"},{PublicName: "category", WireName: "category", Location: "query"},{PublicName: "cv", WireName: "cv", Location: "query"},{PublicName: "data", WireName: "data", Location: "query"},{PublicName: "dma", WireName: "dma", Location: "query"},{PublicName: "ec_mode", WireName: "ec_mode", Location: "query"},{PublicName: "eme", WireName: "eme", Location: "query"},{PublicName: "en", WireName: "en", Location: "query"},{PublicName: "fmt", WireName: "fmt", Location: "query"},{PublicName: "frm", WireName: "frm", Location: "query"},{PublicName: "fst", WireName: "fst", Location: "query"},{PublicName: "gap.gtb", WireName: "gap.gtb", Location: "query"},{PublicName: "gcd", WireName: "gcd", Location: "query"},{PublicName: "gcl_ctr", WireName: "gcl_ctr", Location: "query"},{PublicName: "gcs", WireName: "gcs", Location: "query"},{PublicName: "gtm", WireName: "gtm", Location: "query"},{PublicName: "gtm_ee", WireName: "gtm_ee", Location: "query"},{PublicName: "guid", WireName: "guid", Location: "query"},{PublicName: "hn", WireName: "hn", Location: "query"},{PublicName: "id", WireName: "id", Location: "path"},{PublicName: "label", WireName: "label", Location: "query"},{PublicName: "npa", WireName: "npa", Location: "query"},{PublicName: "pscdl", WireName: "pscdl", Location: "query"},{PublicName: "random", WireName: "random", Location: "query"},{PublicName: "rcb", WireName: "rcb", Location: "query"},{PublicName: "tag_exp", WireName: "tag_exp", Location: "query"},{PublicName: "tiba", WireName: "tiba", Location: "query"},{PublicName: "u_h", WireName: "u_h", Location: "query"},{PublicName: "u_w", WireName: "u_w", Location: "query"},{PublicName: "uaa", WireName: "uaa", Location: "query"},{PublicName: "uab", WireName: "uab", Location: "query"},{PublicName: "uafvl", WireName: "uafvl", Location: "query"},{PublicName: "uam", WireName: "uam", Location: "query"},{PublicName: "uamb", WireName: "uamb", Location: "query"},{PublicName: "uap", WireName: "uap", Location: "query"},{PublicName: "uapv", WireName: "uapv", Location: "query"},{PublicName: "uaw", WireName: "uaw", Location: "query"},{PublicName: "url", WireName: "url", Location: "query"},{PublicName: "userId", WireName: "userId", Location: "query"}, }, []string{"id", }),
	)
	s.AddTool(
		mcplib.NewTool("rum_create_rum",
			mcplib.WithDescription("POST /api/v2/rum. Optional: _dd.api, batch_time, dd-evp-encoding (plus 4 more). Returns the new rum."),
			mcplib.WithString("_dd.api", mcplib.Description("")),
			mcplib.WithNumber("batch_time", mcplib.Description("")),
			mcplib.WithString("dd-evp-encoding", mcplib.Description("")),
			mcplib.WithString("dd-evp-origin", mcplib.Description("")),
			mcplib.WithString("dd-evp-origin-version", mcplib.Description("")),
			mcplib.WithString("dd-request-id", mcplib.Description("")),
			mcplib.WithString("ddsource", mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/api/v2/rum", []mcpParamBinding{{PublicName: "_dd.api", WireName: "_dd.api", Location: "query"},{PublicName: "batch_time", WireName: "batch_time", Location: "query"},{PublicName: "dd-evp-encoding", WireName: "dd-evp-encoding", Location: "query"},{PublicName: "dd-evp-origin", WireName: "dd-evp-origin", Location: "query"},{PublicName: "dd-evp-origin-version", WireName: "dd-evp-origin-version", Location: "query"},{PublicName: "dd-request-id", WireName: "dd-request-id", Location: "query"},{PublicName: "ddsource", WireName: "ddsource", Location: "query"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("targeting-service_list_adinfo",
			mcplib.WithDescription("GET /targeting-service/v3/adinfo. Optional: _, adProvider, app (plus 11 more). Returns the adinfo."),
			mcplib.WithNumber("_", mcplib.Description("")),
			mcplib.WithString("adProvider", mcplib.Description("")),
			mcplib.WithBoolean("app", mcplib.Description("")),
			mcplib.WithBoolean("canTrack", mcplib.Description("")),
			mcplib.WithString("dateEnd", mcplib.Description("")),
			mcplib.WithString("dateStart", mcplib.Description("")),
			mcplib.WithNumber("dest", mcplib.Description("")),
			mcplib.WithString("locResolver", mcplib.Description("")),
			mcplib.WithNumber("origin", mcplib.Description("")),
			mcplib.WithString("pageName", mcplib.Description("")),
			mcplib.WithString("ppid", mcplib.Description("")),
			mcplib.WithNumber("siteId", mcplib.Description("")),
			mcplib.WithString("source", mcplib.Description("")),
			mcplib.WithString("uuid", mcplib.Description("")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/targeting-service/v3/adinfo", []mcpParamBinding{{PublicName: "_", WireName: "_", Location: "query"},{PublicName: "adProvider", WireName: "adProvider", Location: "query"},{PublicName: "app", WireName: "app", Location: "query"},{PublicName: "canTrack", WireName: "canTrack", Location: "query"},{PublicName: "dateEnd", WireName: "dateEnd", Location: "query"},{PublicName: "dateStart", WireName: "dateStart", Location: "query"},{PublicName: "dest", WireName: "dest", Location: "query"},{PublicName: "locResolver", WireName: "locResolver", Location: "query"},{PublicName: "origin", WireName: "origin", Location: "query"},{PublicName: "pageName", WireName: "pageName", Location: "query"},{PublicName: "ppid", WireName: "ppid", Location: "query"},{PublicName: "siteId", WireName: "siteId", Location: "query"},{PublicName: "source", WireName: "source", Location: "query"},{PublicName: "uuid", WireName: "uuid", Location: "query"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("trvl-px_create_pix",
			mcplib.WithDescription("POST /trvl-px/v2/pix. Optional: eapid, emailHash, expUserId (plus 4 more). Returns the new pix."),
			mcplib.WithString("eapid", mcplib.Description("")),
			mcplib.WithString("emailHash", mcplib.Description("")),
			mcplib.WithString("expUserId", mcplib.Description("")),
			mcplib.WithString("guid", mcplib.Description("")),
			mcplib.WithString("inAuthId", mcplib.Description("")),
			mcplib.WithString("serverPlatform", mcplib.Description("")),
			mcplib.WithNumber("tpid", mcplib.Description("")),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/trvl-px/v2/pix", []mcpParamBinding{{PublicName: "eapid", WireName: "eapid", Location: "query"},{PublicName: "emailHash", WireName: "emailHash", Location: "query"},{PublicName: "expUserId", WireName: "expUserId", Location: "query"},{PublicName: "guid", WireName: "guid", Location: "query"},{PublicName: "inAuthId", WireName: "inAuthId", Location: "query"},{PublicName: "serverPlatform", WireName: "serverPlatform", Location: "query"},{PublicName: "tpid", WireName: "tpid", Location: "query"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("typeahead_list__las _vegas, _n_v, _united _states of _america (_l_a_s__harry _reid _intl.)",
			mcplib.WithDescription("GET /api/v4/typeahead/Las Vegas, NV, United States of America (LAS-Harry Reid Intl.). Optional: browser, client, dest (plus 13 more). Returns the Las Vegas, NV, United States of America (LAS_Harry Reid Intl.)."),
			mcplib.WithString("browser", mcplib.Description("")),
			mcplib.WithString("client", mcplib.Description("")),
			mcplib.WithBoolean("dest", mcplib.Description("")),
			mcplib.WithString("device", mcplib.Description("")),
			mcplib.WithNumber("expuserid", mcplib.Description("")),
			mcplib.WithString("features", mcplib.Description("")),
			mcplib.WithString("format", mcplib.Description("")),
			mcplib.WithString("guid", mcplib.Description("")),
			mcplib.WithBoolean("listing", mcplib.Description("")),
			mcplib.WithString("lob", mcplib.Description("")),
			mcplib.WithString("locale", mcplib.Description("")),
			mcplib.WithNumber("maxresults", mcplib.Description("")),
			mcplib.WithBoolean("personalize", mcplib.Description("")),
			mcplib.WithBoolean("recentHistoryEnabled", mcplib.Description("")),
			mcplib.WithNumber("regiontype", mcplib.Description("")),
			mcplib.WithNumber("siteid", mcplib.Description("")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("GET", "/api/v4/typeahead/Las Vegas, NV, United States of America (LAS-Harry Reid Intl.)", []mcpParamBinding{{PublicName: "browser", WireName: "browser", Location: "query"},{PublicName: "client", WireName: "client", Location: "query"},{PublicName: "dest", WireName: "dest", Location: "query"},{PublicName: "device", WireName: "device", Location: "query"},{PublicName: "expuserid", WireName: "expuserid", Location: "query"},{PublicName: "features", WireName: "features", Location: "query"},{PublicName: "format", WireName: "format", Location: "query"},{PublicName: "guid", WireName: "guid", Location: "query"},{PublicName: "listing", WireName: "listing", Location: "query"},{PublicName: "lob", WireName: "lob", Location: "query"},{PublicName: "locale", WireName: "locale", Location: "query"},{PublicName: "maxresults", WireName: "maxresults", Location: "query"},{PublicName: "personalize", WireName: "personalize", Location: "query"},{PublicName: "recentHistoryEnabled", WireName: "recentHistoryEnabled", Location: "query"},{PublicName: "regiontype", WireName: "regiontype", Location: "query"},{PublicName: "siteid", WireName: "siteid", Location: "query"}, }, []string{ }),
	)
	s.AddTool(
		mcplib.NewTool("uisprime_create_track",
			mcplib.WithDescription("POST /api/uisprime/track. Returns the new track."),
			mcplib.WithDestructiveHintAnnotation(false),
			mcplib.WithOpenWorldHintAnnotation(true),
		),
		makeAPIHandler("POST", "/api/uisprime/track", []mcpParamBinding{ }, []string{ }),
	)
	// Search tool — faster than iterating list endpoints for finding specific items
	s.AddTool(
		mcplib.NewTool("search",
			mcplib.WithDescription("Full-text search across all synced data. Faster than paginating list endpoints. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("Search query (supports FTS5 syntax: AND, OR, NOT, quotes for phrases)")),
			mcplib.WithNumber("limit", mcplib.Description("Max results (default 25)")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSearch,
	)
	// SQL tool — ad-hoc analysis on synced data without API calls
	s.AddTool(
		mcplib.NewTool("sql",
			mcplib.WithDescription("Run read-only SQL against local database. Use for ad-hoc analysis, aggregations, and joins across synced resources. Requires sync first."),
			mcplib.WithString("query", mcplib.Required(), mcplib.Description("SQL query (SELECT or WITH...SELECT). Tables match resource names.")),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleSQL,
	)

	// Context tool — front-loaded domain knowledge for agents.
	// Call this first to understand the API taxonomy, query patterns, and capabilities.
	s.AddTool(
		mcplib.NewTool("context",
			mcplib.WithDescription("Get API domain context: resource taxonomy, auth requirements, query tips, and unique capabilities. Call this first."),
			mcplib.WithReadOnlyHintAnnotation(true),
			mcplib.WithDestructiveHintAnnotation(false),
		),
		handleContext,
	)

	// Runtime Cobra-tree mirror — exposes every user-facing command that is
	// not already covered by a typed endpoint or framework MCP tool.
	cobratree.RegisterAll(s, cli.RootCmd(), cobratree.SiblingCLIPath)
}

type mcpParamBinding struct {
	PublicName string
	WireName   string
	Location   string
}

// makeAPIHandler creates a generic MCP tool handler for an API endpoint.
func makeAPIHandler(method, pathTemplate string, bindings []mcpParamBinding, positionalParams []string) server.ToolHandlerFunc {
	return func(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
		c, err := newMCPClient()
		if err != nil {
			return mcplib.NewToolResultError(err.Error()), nil
		}

		// mcp-go v0.47+ made CallToolParams.Arguments an `any` to support
		// non-map payloads; GetArguments() returns the map[string]any shape
		// we rely on here (or an empty map when the payload is something else).
		args := req.GetArguments()

		// positionalParams mixes real URL path params with CLI positional
		// args that map to query params (e.g. `search <query>` -> ?query=);
		// the placeholder check below disambiguates them at runtime.
		path := pathTemplate
		knownArgs := make(map[string]bool, len(bindings))
		pathParams := make(map[string]bool, len(positionalParams))
		params := make(map[string]string)
		bodyArgs := make(map[string]any)
		for _, binding := range bindings {
			knownArgs[binding.PublicName] = true
			v, ok := args[binding.PublicName]
			if !ok {
				continue
			}
			switch binding.Location {
			case "path":
				placeholder := "{" + binding.WireName + "}"
				pathParams[binding.PublicName] = true
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			case "body":
				bodyArgs[binding.WireName] = v
			default:
				params[binding.WireName] = fmt.Sprintf("%v", v)
			}
		}
		for _, p := range positionalParams {
			placeholder := "{" + p + "}"
			if !strings.Contains(pathTemplate, placeholder) {
				continue
			}
			pathParams[p] = true
			if v, ok := args[p]; ok {
				path = strings.Replace(path, placeholder, fmt.Sprintf("%v", v), 1)
			}
		}

		for k, v := range args {
			if pathParams[k] || knownArgs[k] {
				continue
			}
			switch method {
			case "POST", "PUT", "PATCH":
				bodyArgs[k] = v
			default:
				params[k] = fmt.Sprintf("%v", v)
			}
		}

		var data json.RawMessage
		switch method {
		case "GET":
			data, err = c.Get(path, params)
		case "POST":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Post(path, body)
		case "PUT":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Put(path, body)
		case "PATCH":
			body, _ := json.Marshal(bodyArgs)
			data, _, err = c.Patch(path, body)
		case "DELETE":
			data, _, err = c.Delete(path)
		default:
			return mcplib.NewToolResultError("unsupported method: " + method), nil
		}

		if err != nil {
			msg := err.Error()
			switch {
			case strings.Contains(msg, "HTTP 409"):
				return mcplib.NewToolResultText("already exists (no-op)"), nil
			case strings.Contains(msg, "HTTP 400") && cliutil.LooksLikeAuthError(msg):
				return mcplib.NewToolResultError("authentication error: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: the API rejected the request — this usually means auth is missing or invalid." +
					"\n      Set your API key: export BROWSER_INTAKE_US5_DATADOGHQ_API_KEY=<your-key>" +
					"\n      Run 'expedia-cars-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 401"):
				return mcplib.NewToolResultError("authentication failed: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: check your API key." +
					"\n      Set it with: export BROWSER_INTAKE_US5_DATADOGHQ_API_KEY=<your-key>" +
					"\n      Run 'expedia-cars-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 403"):
				return mcplib.NewToolResultError("permission denied: " + cliutil.SanitizeErrorBody(msg) +
					"\nhint: your credentials are valid but lack access to this resource." +
					"\n      Set it with: export BROWSER_INTAKE_US5_DATADOGHQ_API_KEY=<your-key>" +
					"\n      Run 'expedia-cars-pp-cli doctor' to check auth status."), nil
			case strings.Contains(msg, "HTTP 404"):
				if method == "DELETE" {
					return mcplib.NewToolResultText("already deleted (no-op)"), nil
				}
				return mcplib.NewToolResultError("not found: " + msg), nil
			case strings.Contains(msg, "HTTP 429"):
				return mcplib.NewToolResultError("rate limited: " + msg), nil
			default:
				return mcplib.NewToolResultError(msg), nil
			}
		}

		// For GET responses, wrap bare arrays with count metadata
		if method == "GET" {
			trimmed := strings.TrimSpace(string(data))
			if len(trimmed) > 0 && trimmed[0] == '[' {
				var items []json.RawMessage
				if json.Unmarshal(data, &items) == nil {
					wrapped := map[string]any{
						"count": len(items),
						"items": items,
					}
					out, _ := json.Marshal(wrapped)
					return mcplib.NewToolResultText(string(out)), nil
				}
			}
		}
		return mcplib.NewToolResultText(string(data)), nil
	}
}

func newMCPClient() (*client.Client, error) {
	home, _ := os.UserHomeDir()
	cfgPath := filepath.Join(home, ".config", "expedia-cars-pp-cli", "config.toml")
	cfg, err := config.Load(cfgPath)
	if err != nil {
		return nil, fmt.Errorf("loading config: %w", err)
	}
	c := client.New(cfg, 30*time.Second, 2)
	// Agents calling through MCP need fresh data every call. The on-disk
	// response cache survives across MCP server invocations, so a
	// DELETE/PATCH followed by a GET would otherwise return the
	// pre-mutation snapshot for up to the cache TTL. The interactive CLI
	// constructs its own client and is unaffected.
	c.NoCache = true
	return c, nil
}

func dbPath() string {
	home, _ := os.UserHomeDir()
	return filepath.Join(home, ".local", "share", "expedia-cars-pp-cli", "data.db")
}
// Note: MCP tools use their own dbPath() because they are in a separate package (main, not cli).
// The CLI's defaultDBPath() in the cli package uses the same canonical path.

func handleSearch(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	limit := 25
	if v, ok := args["limit"].(float64); ok && v > 0 {
		limit = int(v)
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	results, err := db.Search(query, limit)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("search failed: %v", err)), nil
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// validateReadOnlyQuery gates the MCP sql tool. The agent contract advertised
// to the host is ReadOnlyHintAnnotation(true); a false annotation on a
// mutating tool lets MCP hosts auto-approve writes and is treated as a real
// bug per the project's agent-native security model.
//
// The gate is an allowlist (SELECT or WITH only) applied AFTER stripping the
// leading whitespace, line comments, block comments, and semicolons that
// SQLite itself ignores before parsing. A naive HasPrefix check on a
// keyword blocklist is bypassable by prefixing the dangerous statement with
// "/* x */" or "-- x\n" — TrimSpace strips outer whitespace but does not
// understand SQL comment syntax. Combined with the empirical fact that
// modernc.org/sqlite's mode=ro does NOT block VACUUM INTO (writes a snapshot
// to a new file) or ATTACH DATABASE (opens a separate writable handle),
// such a bypass produces silent exfiltration to an attacker-chosen path.
//
// SELECT and WITH are the only allowed leading keywords. WITH supports
// SELECT-form CTEs; CTE-wrapped writes ("WITH x AS (...) INSERT ...") are
// caught by OpenReadOnly's mode=ro one layer down. PRAGMA, ATTACH, VACUUM,
// and every other DDL/DML keyword fail at this gate before reaching SQLite.
func validateReadOnlyQuery(query string) error {
	upper := strings.ToUpper(stripLeadingSQLNoise(query))
	if !strings.HasPrefix(upper, "SELECT") && !strings.HasPrefix(upper, "WITH") {
		return fmt.Errorf("only SELECT queries are allowed")
	}
	return nil
}

// stripLeadingSQLNoise removes leading whitespace, SQL line comments
// (-- to end of line), block comments (/* ... */), and statement
// separators (;) from query. SQLite skips these before parsing the first
// keyword, so a security gate that does not strip them mismatches what the
// driver actually executes.
func stripLeadingSQLNoise(query string) string {
	for {
		query = strings.TrimLeft(query, " \t\r\n;")
		switch {
		case strings.HasPrefix(query, "--"):
			if idx := strings.IndexByte(query, '\n'); idx >= 0 {
				query = query[idx+1:]
				continue
			}
			return ""
		case strings.HasPrefix(query, "/*"):
			if idx := strings.Index(query[2:], "*/"); idx >= 0 {
				query = query[2+idx+2:]
				continue
			}
			return ""
		default:
			return query
		}
	}
}

func handleSQL(ctx context.Context, req mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	args := req.GetArguments()
	query, ok := args["query"].(string)
	if !ok || query == "" {
		return mcplib.NewToolResultError("query is required"), nil
	}

	if err := validateReadOnlyQuery(query); err != nil {
		return mcplib.NewToolResultError(err.Error()), nil
	}

	db, err := store.OpenReadOnly(dbPath())
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("opening database: %v", err)), nil
	}
	defer db.Close()

	rows, err := db.Query(query)
	if err != nil {
		return mcplib.NewToolResultError(fmt.Sprintf("query failed: %v", err)), nil
	}
	defer rows.Close()

	cols, _ := rows.Columns()
	var results []map[string]any
	for rows.Next() {
		values := make([]any, len(cols))
		ptrs := make([]any, len(cols))
		for i := range values {
			ptrs[i] = &values[i]
		}
		rows.Scan(ptrs...)
		row := make(map[string]any)
		for i, col := range cols {
			row[col] = values[i]
		}
		results = append(results, row)
	}

	data, _ := json.MarshalIndent(results, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

func handleContext(_ context.Context, _ mcplib.CallToolRequest) (*mcplib.CallToolResult, error) {
	ctx := map[string]any{
		"api":         "expedia-cars",
		"description": "Discovered API spec for browser-intake-us5-datadoghq",
		"archetype":   "content",
		"tool_count":  18,
		// tool_surface tells agents which surface a capability lives on.
		"tool_surface": "MCP exposes typed endpoint tools plus a runtime mirror of user-facing CLI commands. Endpoint tools keep typed schemas; command-mirror tools shell out to the companion expedia-cars-pp-cli binary.",
		"auth": map[string]any{
			"type": "api_key",
			"env_vars": []map[string]any{
				{
					"name": "BROWSER_INTAKE_US5_DATADOGHQ_API_KEY",
					"kind": "per_call",
					"required": true,
					"sensitive": true,
					"description": "Set to your API credential.",
				},
			},
		},
		"resources": []map[string]any{
			{
				"name": ".well-known",
				"description": "Operations on web-identity",
				"endpoints": []string{"list_web_identity",  },
				"syncable": true,
			},
			{
				"name": "cl",
				"description": "Operations on 2x2.json",
				"endpoints": []string{"create_2x2.json",  },
			},
			{
				"name": "ds",
				"description": "Operations on pwa",
				"endpoints": []string{"get_pwa",  },
				"searchable": true,
			},
			{
				"name": "egcs",
				"description": "Operations on collect",
				"endpoints": []string{"create_collect",  },
			},
			{
				"name": "getconfig",
				"description": "Operations on sodar",
				"endpoints": []string{"list_sodar",  },
				"syncable": true,
				"searchable": true,
			},
			{
				"name": "graphql",
				"description": "Operations on graphql",
				"endpoints": []string{"create_graphql",  },
			},
			{
				"name": "gsi",
				"description": "Operations on status",
				"endpoints": []string{"list_listaccounts", "list_passive.json", "list_status",  },
				"syncable": true,
				"searchable": true,
			},
			{
				"name": "k4AZ50DsywoM",
				"description": "Operations on NpJ2or",
				"endpoints": []string{"create_NpJ2or",  },
			},
			{
				"name": "maps",
				"description": "Operations on js",
				"endpoints": []string{"list_gen_204", "list_js",  },
				"syncable": true,
				"searchable": true,
			},
			{
				"name": "pagead",
				"description": "Operations on conversion",
				"endpoints": []string{"get_conversion",  },
				"searchable": true,
			},
			{
				"name": "rum",
				"description": "Operations on rum",
				"endpoints": []string{"create_rum",  },
			},
			{
				"name": "targeting-service",
				"description": "Operations on adinfo",
				"endpoints": []string{"list_adinfo",  },
				"syncable": true,
				"searchable": true,
			},
			{
				"name": "trvl-px",
				"description": "Operations on pix",
				"endpoints": []string{"create_pix",  },
			},
			{
				"name": "typeahead",
				"description": "Operations on Las Vegas, NV, United States of America (LAS-Harry Reid Intl.)",
				"endpoints": []string{"list_Las Vegas, NV, United States of America (LAS_Harry Reid Intl.)",  },
				"syncable": true,
				"searchable": true,
			},
			{
				"name": "uisprime",
				"description": "Operations on track",
				"endpoints": []string{"create_track",  },
			},
		},
		"query_tips": []string{
			"Pagination uses cursor-based paging. Pass after parameter for subsequent pages.",
			"Control page size with the limit parameter (default 100).",
			"Use the sql tool for ad-hoc analysis on synced data. Run sync first to populate the local database.",
			"Use the search tool for full-text search across all synced resources. Faster than iterating list endpoints.",
			"Prefer sql/search over repeated API calls when the data is already synced.",
		},
	}
	data, _ := json.MarshalIndent(ctx, "", "  ")
	return mcplib.NewToolResultText(string(data)), nil
}

// RegisterNovelFeatureTools is kept as a compatibility no-op for older MCP
// mains. New generated mains call RegisterTools only; RegisterTools now
// includes the runtime Cobra-tree mirror.
func RegisterNovelFeatureTools(s *server.MCPServer) {
	_ = s
}
